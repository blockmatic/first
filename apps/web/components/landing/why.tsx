export function Why() {
  return (
    <section id="why" className="border-b border-border px-6 py-20">
      <p className="font-mono text-xs tracking-[0.3em] uppercase text-muted-foreground">Why</p>
      <h2 className="font-display mt-3 text-3xl md:text-4xl">
        Some decisions are too consequential to be afterthoughts
      </h2>
      <p className="mt-4 max-w-2xl text-muted-foreground">
        AI agents need a product. Everyone encodes that differently — or not at all — until the
        codebase becomes the brief.
      </p>
      <p className="mt-8 max-w-xl font-display text-2xl leading-snug">
        FIRST is shared language between agents and domain experts. Product and system design land
        as files before code. Named FIRST because it is the first thing you do.
      </p>
    </section>
  )
}
