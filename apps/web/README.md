# @repo/web

Public FIRST site: landing, articles, operational templates, and maintainer docs.

## Commands

```bash
pnpm --filter @repo/web dev
pnpm --filter @repo/web build
```

Articles and specs are read from `../../_first/` at build time. Do not fork that markdown into `content/`.

See [Development](/docs/development) on the running site.
